namespace Tutorial_6
{
    class Program
    {
        static void Main()
        {
            int zahl = 0;

            while (zahl < 100)
            {
                zahl++;  // es geht auch zahl += 1;
                Console.WriteLine(zahl);
            }

        }
    }
}
